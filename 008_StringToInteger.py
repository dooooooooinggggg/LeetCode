import re


class Solution:
    def myAtoi(self, str):
        """
        :type str: str
        :rtype: int
        """
        s = str
        for i in range(len(s)):
            if(s[i] == " " or)
